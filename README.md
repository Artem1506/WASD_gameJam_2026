# Игра «Зеленый Змей» (WASD GameJam 2026)

Репозиторий проекта, разработанного для участия в геймджеме **«WASD»**.

- **Даты проведения джема**: 25.05.2026 — 07.06.2026
- **Движок**: GameMaker Studio 2 v2024.14.4.222 (и выше)
<<<<<<< Updated upstream
- **Целевые платформы**: HTML5 (Web) и Windows (Desktop)
=======
- **Целевые платформы**: Windows (Desktop), HTML5 (Web), Android (Mobile)
>>>>>>> Stashed changes
- **Команда**: GrampsGamer

---

## 👥 Команда разработки

1. **Артем Ваулин** — Продюсер, геймдизайнер, программист
2. **Василий Казаков** — Композитор, саунд-дизайнер
3. **Павел Artmoogl** — Художник, аниматор
<<<<<<< Updated upstream
* **RodjerX** — Плейтесты 

---

## 🎮 Концепция и Игровой процесс

**«Зеленый Змей»** — это хардкорный survival-экшен на базе классической механики «Змейка», переосмысленной через геймдизайнерскую призму **нестабильного управления (Input Lag)**. 

### Основные механики:
* **Опьянение и лаг ввода**: Сбор испорченного винограда повышает уровень опьянения, что увеличивает задержку реакции змейки на команды WASD (возникает задержка ввода). Игроку приходится рассчитывать повороты заранее с учетом изменяющегося лага.
* **Экспоненциальное ускорение**: Физическая скорость змейки плавно возрастает по мере роста очков.
* **Механика Combo (Risk-Reward)**: Накопление комбо-очков за непрерывный сбор полезных ягод временно замораживает рост лага (комбо 5) или полностью обнуляет его (комбо 10).
* **Локальный мультиплеер (1v1)**: Соревновательный режим на одной арене со взаимными столкновениями хвостов и раздельным расчетом лага для каждого игрока.

### 🍎 Игровые предметы:
* **Свежий виноград: +1 очко к счету, +1 к комбо.
* **Испорченный виноград: +1 к уровню опьянения (увеличивает лаг), сброс комбо.
* **Таблетка: снижает уровень опьянения на 3 пункта (уменьшает лаг).
* **Защитная каска: поглощает последствия одной аварии (блокирует рост лага и сброс комбо).
* **Мусорный бак: очищает 50% случайных испорченных ягод на поле.
* **Банан: +3 очка/комбо, но удваивает скорость движения на 3 секунды.

### ⌨️ Управление:
* **Игрок 1**: клавиши `W`, `A`, `S`, `D` для движения.
* **Игрок 2**: клавиши-стрелки.
* **Клавиша `Esc`**: пауза.

---

## 🛠 Технические особенности и оптимизация HTML5

Проект адаптирован для бесшовной работы как на ПК, так и в браузере (HTML5).

### ⏸️ Стабильность меню паузы и шейдеров
* **Сортировка слоев при рисовании**: Объект `obj_pause` динамически переопределяет глубину на `depth = 150` в событии `Create`, благодаря чему размытый скриншот игры (`pause_surf`) и черное перекрытие гарантированно отрисовываются позади кнопок интерфейса (`HUD`, глубина `100`), не закрывая их.
* **Защита от крашей WebGL при смене комнат**: Во избежание зависаний HTML5-рантайма при переходе из меню паузы в комнату результатов, в кнопке `obj_buttonToResult` перед вызовом `room_goto` принудительно восстанавливается активность всех деактивированных объектов (`instance_activate_all()`).
* **Корректные шейдеры**: Шейдеры размытия (`sh_blur`), затемнения (`sh_blackout`) и лага (`sh_inputLag`) приведены к WebGL-совместимому синтаксису (GLSL ES) с корректными объявлениями точности `precision`.

---

## 📁 Структура проекта

```text
sprites/   - графические ассеты и спрайты интерфейса
objects/   - управляющие контроллеры и интерактивные объекты
rooms/     - уровни игры (rm_start, rm_main, rm_2Players, rm_result)
audio/     - музыкальное сопровождение и звуковые эффекты
scripts/   - скрипты логики ввода, движения змейки и ресайза
shaders/   - графические шейдеры (размытие, лаг, blackout)
fonts/     - игровые шрифты (поддержка кириллицы)
```

---

## 📝 Правила нейминга ассетов

Каждый ассет в проекте должен иметь соответствующий префикс:
=======
* **RodjerX** — QA / Плейтесты ([Телеграм-канал](https://t.me/myArduinoProj))

---

## 🌿 Обзор ветвей разработки (Git Branches)

В репозитории проекта ведется разработка под три ключевые платформы, распределенная по веткам:

1. **`main` (Windows / Core)**:
   - Базовая сборка под Windows.
   - Содержит основную логику змейки, алгоритмы движения, комбо-систему, генерацию винограда, препятствий и шейдер лага ввода `sh_inputLag`.
   - Включает локальный мультиплеер 1v1 (`rm_2Players`) и систему рекордов.
2. **`HTML5` (Web)**:
   - Адаптация под браузерный запуск.
   - Внедряет JS-расширение `html5` и управляющий объект `oResize` для масштабирования игрового холста под размеры окна браузера.
   - Оптимизирует шейдеры под WebGL (GLSL ES) и решает проблемы с утечкой памяти и зависанием WebGL-контекста во время паузы/смены комнат.
3. **`Android` (Mobile)**:
   - Версия для мобильных устройств.
   - Добавляет сенсорное управление: объект `obj_virtualButton` разделяет экран на 4 невидимые сенсорные зоны поворота и выводит кнопку выхода `ui_buttonExit`.
   - Настраивает параметры компиляции в `options_android.yy` (поддержка архитектуры ARM64, target SDK 35, интеграция Gradle 8.8.0) и добавляет мобильные иконки и сплеш-скрины.

---

## 🎮 Концепция и Игровой процесс

**«Зеленый Змей»** — это хардкорный survival-экшен на базе классической механики «Змейка», переосмысленной через геймдизайнерскую призму **нестабильного управления (Input Lag)**. 

### Основные механики:
* **Опьянение и лаг ввода**: Сбор испорченного винограда повышает уровень опьянения, что увеличивает задержку реакции змейки на команды (Input Lag). Игроку приходится рассчитывать повороты заранее с учетом изменяющегося лага.
* **Экспоненциальное ускорение**: Физическая скорость змейки плавно возрастает по мере роста очков.
* **Механика Combo (Risk-Reward)**: Накопление комбо-очков за непрерывный сбор полезных ягод временно замораживает рост лага (комбо 5) или полностью обнуляет его (комбо 10).
* **Локальный мультиплеер (1v1)**: Соревновательный режим на одной арене со взаимными столкновениями хвостов и раздельным расчетом лага для каждого игрока.

### 🍎 Игровые предметы:
* **Свежий виноград (`spr_grapeFresh`)**: +1 очко к счету, +1 к комбо.
* **Испорченный виноград (`spr_grapeRotten`)**: +1 к уровню опьянения (увеличивает лаг), сброс комбо.
* **Таблетка (`spr_pill`)**: снижает уровень опьянения на 3 пункта (уменьшает лаг).
* **Защитная каска (`spr_helmet`)**: поглощает последствия одной аварии (блокирует рост лага и сброс комбо).
* **Мусорный бак (`spr_cleaner`)**: очищает 50% случайных испорченных ягод на поле.
* **Банан (`spr_banan`)**: +3 очка/комбо, но удваивает скорость движения на 3 секунды.

### ⌨️ Управление:
* **Windows / Web (Игрок 1)**: клавиши `W`, `A`, `S`, `D`.
* **Windows / Web (Игрок 2)**: клавиши-стрелки.
* **Android**: тапы по соответствующим четвертям экрана (сенсорные зоны `obj_virtualButton`).
* **Клавиша `Esc` / Кнопка выхода на Android**: пауза.

---

## 🛠 Технические особенности и оптимизация HTML5

Проект адаптирован для бесшовной работы как на ПК, так и в браузере (HTML5).
>>>>>>> Stashed changes

### 🖥️ Адаптивный ресайз (HTML5 Canvas Scaling)
* **Интеграция JS-расширения**: Подключено расширение `html5` (`extensions/html5/html5.yy`), внедряющее JavaScript-функции `evalJS` и `browser_stretch_canvas` для динамического ресайза холста в браузере.
* **Объект-контроллер `oResize`**: Размещен во всех комнатах игры (`rm_start`, `rm_main`, `rm_2Players`, `rm_result`). Он автоматически подстраивает размеры окна, камер, портов и GUI под размеры окна браузера при изменении его масштаба.
* **Кроссплатформенный GML-fallback**: Создан GML-скрипт `scripts/evalJS/evalJS.gml`. На Windows-сборках он перехватывает вызовы `evalJS` и возвращает размеры стандартного окна GameMaker, предотвращая ошибки компиляции ("unknown function") и падения на десктопе.

### ⏸️ Стабильность меню паузы и шейдеров
* **Сортировка слоев при рисовании**: Объект `obj_pause` динамически переопределяет глубину на `depth = 150` в событии `Create`, благодаря чему размытый скриншот игры (`pause_surf`) и черное перекрытие гарантированно отрисовываются позади кнопок интерфейса (`HUD`, глубина `100`), не закрывая их.
* **Защита от крашей WebGL при смене комнат**: Во избежание зависаний HTML5-рантайма при переходе из меню паузы в комнату результатов, в кнопке `obj_buttonToResult` перед вызовом `room_goto` принудительно восстанавливается активность всех деактивированных объектов (`instance_activate_all()`).
* **WebGL-совместимые шейдеры**: Шейдеры размытия (`sh_blur`), затемнения (`sh_blackout`) и лага (`sh_inputLag`) приведены к WebGL-совместимому синтаксису (GLSL ES) с корректными объявлениями точности `precision`.

---

<<<<<<< Updated upstream
## 💻 Кодстайл

| Элемент | Стиль | Пример |
|---|---|---|
| Скрипты / функции | camelCase | `playEatFresh` |
| Локальные переменные | snake_case с подчеркиванием | `_surf_w` |
| Объекты | camelCasee | `obj_snakeHead` |
| Константы / Макросы | UPPER_SNAKE_CASE | `MAX_LAG` |
| Перечисления (enum) | PascalCase | `GameState` |
| Спрайты / Звуки | camelCase / camelCase | `spr_grapeFresh` / `snd_collision` |
| Шейдеры | camelCase | `sh_blur` |

---

## 🐙 Git Workflow

* **Основная ветка**: `main`
* **Feature-ветки**: Новые задачи выполняются в отдельных ветках `feature/название-фичи` и вливаются через Pull Request.
* **Правило коммитов**: Коммиты должны иметь ясные, лаконичные англоязычные названия (например, `Fix pause menu button depth`, `Add oResize fallback for Windows`).
=======
## 📋 План объединения (Unification Plan) Android, Windows и HTML5

Для того чтобы объединить все три сборки (Windows, HTML5, Android) в одну общую кодовую базу (ветку `main`) и исключить краши и несовместимости, предлагается следующий план имплементации:

### Шаг 1. Слияние (Git Merge) ресурсов и ассетов
* Выполнить слияние веток `Android` и `HTML5` в `main`.
* Разрешить конфликты в файлах проектов `.yyp` так, чтобы расширение `html5`, скрипты ресайза, а также спрайт `ui_buttonExit` и объект `obj_virtualButton` были одновременно зарегистрированы в проекте.
* Все ассеты иконок и сплеш-скринов для Android должны быть сохранены в каталоге `options/android/`.

### Шаг 2. Защита кода от обращений к несуществующим объектам (Runtime Safety)
На сборках Windows/HTML5 объект `obj_virtualButton` отсутствует в комнатах, что сейчас вызывает критический краш игры из-за прямого обращения к его переменным.
* **Ввод в [sc_input.gml](file:///c:/Users/vauli/GameMakerProjects/WASD_gameJam_2026/scripts/sc_input/sc_input.gml)**:
  Заменить прямые проверки вроде `obj_virtualButton.up_down` на безопасные функции проверки существования:
  ```gml
  var _v_up = false, _v_down = false, _v_left = false, _v_right = false;
  if (instance_exists(obj_virtualButton))
  {
      _v_up = obj_virtualButton.up_down;
      _v_down = obj_virtualButton.down_down;
      _v_left = obj_virtualButton.left_down;
      _v_right = obj_virtualButton.right_down;
  }
  pressed_up = keyboard_check_pressed(ord("W")) || _v_up;
  pressed_down = keyboard_check_pressed(ord("S")) || _v_down;
  pressed_left = keyboard_check_pressed(ord("A")) || _v_left;
  pressed_right = keyboard_check_pressed(ord("D")) || _v_right;
  ```
* **Пауза в `obj_pause` ([Step_0.gml](file:///c:/Users/vauli/GameMakerProjects/WASD_gameJam_2026/objects/obj_pause/Step_0.gml))**:
  Заменить прямое чтение `obj_virtualButton.exit_down` на безопасное:
  ```gml
  var _v_exit = false;
  if (instance_exists(obj_virtualButton))
  {
      _v_exit = obj_virtualButton.exit_down;
  }
  if (keyboard_check_pressed(vk_escape) || _v_exit)
  {
      is_paused = !is_paused;
  }
  ```

### Шаг 3. Динамический спавн мобильного управления (Dynamic Initialization)
Чтобы виртуальные кнопки не отображались на Windows и десктопном HTML5, но автоматически появлялись на смартфонах/планшетах:
* Удалить `obj_virtualButton` из редактора комнат (`rm_main` и `rm_2Players`).
* Создать скрипт инициализации или добавить код в `obj_game` (в событие Create):
  ```gml
  // Проверяем, запущена ли игра на мобильной ОС
  var _is_mobile = (os_type == os_android) || (os_type == os_ios);
  
  // Дополнительно проверяем мобильный браузер для HTML5
  if (os_browser != browser_not_a_browser)
  {
      // Если это HTML5, проверяем user-agent на наличие мобильных устройств
      var _ua = string_lower(evalJS("navigator.userAgent"));
      if (string_pos("android", _ua) > 0 || string_pos("iphone", _ua) > 0 || string_pos("ipad", _ua) > 0)
      {
          _is_mobile = true;
      }
  }
  
  // Спавним виртуальный джойстик только для мобильных платформ
  if (_is_mobile)
  {
      if (!instance_exists(obj_virtualButton))
      {
          instance_create_layer(0, 0, "HUD", obj_virtualButton);
      }
  }
  ```

### Шаг 4. Адаптация контроллера ресайза `oResize` под мобильные ОС
На Android/iOS растягивание экрана браузера не требуется, и вызовы JS-кода в `oResize` могут приводить к сбоям.
* **В объекте `oResize` ([Create_0.gml](file:///c:/Users/vauli/GameMakerProjects/WASD_gameJam_2026/objects/oResize/Create_0.gml))**:
  Изолировать выполнение ресайза для мобильных платформ:
  ```gml
  // На нативном Android/iOS используем стандартные функции размеров экрана
  if (os_type == os_android || os_type == os_ios)
  {
      window_set_size(display_get_width(), display_get_height());
      exit;
  }
  
  // Для Windows и HTML5 выполняем стандартную логику адаптивного ресайза
  var bw = real(evalJS("window.innerWidth"));
  var bh = real(evalJS("window.innerHeight"));
  window_set_size(bw, bh);
  view_enabled = true;
  view_visible[0] = true;
  gmcallback_on_browser_resize();
  ```

---

## 📁 Структура проекта и Кодстайл

*(Подробная структура каталогов, форматы именования ресурсов `obj_`, `spr_`, `snd_` и правила ведения Git-репозитория описаны в соответствующих подразделах выше).*
>>>>>>> Stashed changes
